<Window x:Class="CarDeadlineTracker.Views.AddEditCarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CarDeadlineTracker.Views"
        mc:Ignorable="d"
        Title="Add/Edit Car" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="20">
            <Label Content="Number Plate:"/>
            <TextBox Text="{Binding Car.NumberPlate}"/>
            
            <Label Content="Brand:"/>
            <TextBox Text="{Binding Car.Brand}"/>

            <Label Content="Make:"/>
            <TextBox Text="{Binding Car.Make}"/>
            
            <Label Content="Branch Location:"/>
            <TextBox Text="{Binding Car.BranchLocation}"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Save" 
                        Command="{Binding SaveCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                        Margin="0,0,10,0"/>
                <Button Content="Cancel" 
                        Command="{Binding CancelCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
