<Window x:Class="CarDeadlineTracker.Views.GenerateSummaryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CarDeadlineTracker.Views"
        mc:Ignorable="d"
        Title="Generate Summary" Height="250" Width="275">
    <Grid Margin="15, 10">
        <StackPanel>
            <Label Content="Start Date" HorizontalAlignment="Left" 
                           Height="30" VerticalAlignment="Top" 
                           Padding="0,10,0,0" /> 
            <DatePicker SelectedDate="{Binding StartDate}"/>
            <Label Content="End date" HorizontalAlignment="Left" 
                           Height="30" VerticalAlignment="Top" 
                           Padding="0,10,0,0" /> 
            <DatePicker SelectedDate="{Binding EndDate}"/>
            <Button Content="Generate Summary" Height="30" Width="150" Margin="15" 
                    Command="{Binding GenerateSummaryCommand}"/>
        </StackPanel>
    </Grid>
</Window>
